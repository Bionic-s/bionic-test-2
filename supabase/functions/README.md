# Edge Functions

Deployed automatically by `.github/workflows/deploy-supabase-functions.yml`
whenever a push touches this directory. No manual `supabase functions deploy`
needed — the workflow authenticates with the `SUPABASE_ACCESS_TOKEN`
repository secret and deploys every function here to project
`krjgjaemysvutpwbwgst`.
